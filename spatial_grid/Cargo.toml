[package]
edition = "2024"
name = "spatial_grid"
version = "0.1.0"

[dependencies]
geometry = {path = "../geometry"}
vector2d = {path = "../vector2d"}

[dev-dependencies]
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
